<form action="{{ route('login') }}" method="POST" style="text-align: center; display: flex; flex-direction: column; justify-content: center; align-items: center; height: 100vh;">
    @csrf
    <label style="font-size: 18px; font-weight: bold; margin-bottom: 10px;">You are not allowed to access</label>
</form>
